extends layouts/main

block content
    .form-container
        form()
            input(type="text", name="name", id="name" placeholder="Full Name", required=true)
            input(type="email", name="email", id="email'"placeholder="Email", required=true)
            input(type="password", name="password", id="password" placeholder="Password", required=true)
            input(type="password", name="veriPassword", id="veriPassword" placeholder="Verify Password", required=true)
            button(id='joinBtn' type="button", value="Join Now")
